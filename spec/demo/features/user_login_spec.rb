require "spec_helper"

describe "User Login" do
  xit "user can login using the demo app"
  xit "user will stay login when coming back to the app (cookie)"
  xit "The cookie should have an expiry (2 weeks?)"
end